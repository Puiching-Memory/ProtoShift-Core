# Migration

该目录是 ProtoShiftGame 的迁移产物占位骨架。

默认包含：

- `scene-manifest.json`
- `gameplay-spec.json`
- `ui/template-hud.json`
- `snapshots/*.json`
- `host-only-backlog.json`

初始化新项目后，Agent 应在此目录持续更新迁移产物，而不是临时拼接说明文本。